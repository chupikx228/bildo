import json

from src.apps.schemas import AppDocument
from src.generation.json_schema import to_strict_json_schema
from src.generation.llm_client import ChatMessage, JsonSchema

SCHEMA_NAME = "app_document"

SCREEN_WIDTH = 370
SCREEN_HEIGHT = 640

DESIGN_RULES = f"""Дизайн-принципы для генерации экранов.

ИЗБЕГАЙ этих паттернов — это дефолты, которые модели выдают чаще всего, и по ним сразу видно AI-slop:
- Один и тот же borderRadius на всех View/Button/Image в приложении, независимо от их роли.
- Одинаковая полупрозрачная тень (что-то вроде rgba(0,0,0,0.1)) под каждой карточкой — если тени вообще доступны в схеме, используй их точечно, не на всём.
- Сетка из одинаковых карточек одинакового размера как единственный способ показать список — меняй размеры узлов по важности содержимого, а не только по данным.
- Тёплый кремовый фон + terracotta/оранжевый акцент, ИЛИ почти чёрный фон + один неоновый акцент — это два самых частых дефолта LLM. Подбирай палитру от темы приложения, а не из этих двух вариантов.
- Контент, зацентрованный вертикально и горизонтально на каждом экране без исключений — используй асимметрию: элемент может начинаться от левого края, а не от центра сцены.
- ВСЕ ЗАГЛАВНЫЕ для лейблов/эйбрау, разделители вида "A · B · C", стрелка "→" в конце текста кнопки — не добавляй это, если пользователь явно не просил.
- Текст placeholder вида "Lorem ipsum", "Заголовок", "Описание" — придумывай реальный контент под тематику приложения из запроса.

ПРИМЕНЯЙ:
- Палитра — 10 токенов темы должны быть выбраны под смысл приложения (фитнес-трекер, рецепты, финансы — разная эмоция), не универсальный набор. У каждого токена должна быть понятная роль (фон экрана, фон поверхности/карточки, основной текст, вторичный текст, акцент, текст на акценте, граница/разделитель, успех, ошибка, дополнительный/приглушённый). Контраст текст/фон проверяй на глаз, не спускай ниже читаемого.
- Иерархия размером и положением, а не декором: на каждом экране один элемент должен быть визуально главным (крупный заголовок, крупное изображение, крупная цифра) — считай его "hero" экрана, остальное — тише и мельче.
- Используй возможности абсолютного позиционирования: элементы могут выходить к краю сцены (x=0 или x+width={SCREEN_WIDTH}), накладываться друг на друга внутри одного родителя (например, Text поверх Image), это не баг компоновки, а инструмент.
- Оставляй "воздух" — не заполняй сцену узлами плотно; пустое пространство вокруг hero-элемента делает его заметнее.
- Кнопки называй по действию, которое произойдёт ("Сохранить", "Добавить рецепт"), а не универсальным "Отправить"/"ОК".
- Пустые состояния (пустой список, ScrollView без контента) оформляй как приглашение к действию с понятным текстом, а не оставляй пустым View."""

DESIGN_SELF_CHECK = """Перед тем как вернуть JSON, мысленно проверь: если бы я сгенерировал экран для другого запроса на эту же тему, получился бы тот же layout и та же палитра? Если да — измени."""

DESIGN_VARIETY = """Перед генерацией документа определи дизайн-направление ЭТОГО конкретного приложения.

Шаг 1. Извлеки характер из запроса пользователя: для кого приложение, какая у него эмоция (серьёзное/игривое/спокойное/энергичное/роскошное/минимальное), что упомянул пользователь буквально (материалы, цвета, референсы, сравнения "как у X").  Если пользователь дал явные сигналы — они всегда важнее любого шаблона ниже.

Шаг 2. Выбери по одному варианту на каждой оси, обоснуй выбор через характер из шага 1 (не через жанр приложения):

Layout-архетип: edge-to-edge hero (один крупный элемент на весь верх экрана) / bento-grid (блоки разного размера, плотная мозаика) / editorial-list (текстовый список с акцентной типографикой, мало декора) / dashboard (много мелких блоков данных) / split-focus (экран разделён на две смысловые зоны) / conversational (вертикальный поток реплик/карточек) / carousel-first (один горизонтальный ряд как главный элемент экрана).

Цветовая логика: monochrome + один акцент / dual-tone (два равнозначных цвета в напряжении) / dark-canvas с несколькими яркими акцентами / soft-tonal (близкие по светлоте оттенки одного тона) / high-contrast duotone. Конкретные HEX подбирай под характер приложения, не под название логики.

Плотность: minimal-breathing (много пустого пространства, мало элементов на экране) / dense-utility (много данных, мелкие узлы, для приложений про информацию, не про впечатление) / rhythmic-mix (чередование плотных и пустых зон на разных экранах).

Роль изображений (если есть Image-узлы в контенте): фото как фон-подложка под текст / фото как отдельный крупный акцентный блок / фото как мелкие превью в ряду / изображений нет вовсе, акцент на типографике.

Шаг 3. Прежде чем писать JSON, коротко (для себя, не в ответ) сформулируй одним предложением, чем дизайн-направление этого приложения отличается от «дефолтного» приложения этой же категории. Если не можешь сформулировать отличие — вернись к шагу 2 и выбери другую комбинацию.

Не тяготей к одной и той же комбинации осей просто потому что она "безопасная" — за много запросов подряд комбинации должны заметно различаться даже для похожих категорий приложений."""

RULES = f"""Ты генератор мобильных приложений для конструктора Bildo.
По описанию пользователя ты собираешь документ приложения `AppDocument` и возвращаешь его одним JSON-объектом.

Формат ответа:
- только JSON-объект документа, без markdown-ограждений, без пояснений до или после;
- ключи в camelCase ровно так, как в JSON Schema ниже;
- в ответе обязаны присутствовать ВСЕ ключи из JSON Schema ниже — для поля, для которого нет данных, ставь `null`, не опускай ключ;
- поля `id`, `createdAt`, `updatedAt` обязательны в схеме, но сервер их перезапишет: положи любые валидные строки;
- поле `revision` обязательно в схеме, но сервер его перезапишет: положи `1`.

Правила модели документа:
- Сцена экрана — {SCREEN_WIDTH} x {SCREEN_HEIGHT} точек. Всё позиционирование абсолютное: `layout` с `x`, `y`, `width`, `height` в этих координатах.
- Корневой узел каждого экрана — `View` с `layout` ровно 0, 0, {SCREEN_WIDTH}, {SCREEN_HEIGHT}. Координаты вложенных узлов отсчитываются от родителя.
- Узлы не выходят за пределы родителя и не перекрывают друг друга, если это не задумано.
- Восемь типов узлов: View, Text, Button, Image, TextInput, ScrollView, FlatList, Spacer.
- `id` узлов уникальны в пределах всего документа, латиницей через дефис.
- У экранов `route` — голый идентификатор без ведущего слэша. Стартовый экран всегда имеет `id` и `route`, равные `index`.
- `navigation.roots` содержит только существующие `route` экранов.
- Действия `navigate` ведут только на существующие `route`.
- Переменные объявляй в `state`; `textBind`, `valueBind` и действие `setVar` ссылаются только на них.
- Тема — 10 токенов, цвета в HEX. Тема контрастная и читаемая: текст различим на фоне и на кнопках.
- Названия экранов, тексты кнопок и подписи пиши на языке запроса пользователя.

Объём: от 2 до 5 экранов, каждый экран содержательный — заголовок, основной контент и переход на другие экраны."""


def app_document_schema() -> JsonSchema:
    return to_strict_json_schema(AppDocument.model_json_schema(by_alias=True))


def build_system_prompt() -> str:
    schema = json.dumps(app_document_schema(), ensure_ascii=False)
    return f"{DESIGN_RULES}\n\n{DESIGN_SELF_CHECK}\n\n{DESIGN_VARIETY}\n\n{RULES}\n\nJSON Schema документа:\n{schema}"


def build_messages(prompt: str, name: str | None) -> list[ChatMessage]:
    request = f"Собери приложение по описанию:\n{prompt}"
    if name is not None:
        request = f"{request}\n\nНазвание приложения: {name}"
    return [
        ChatMessage(role="system", content=build_system_prompt()),
        ChatMessage(role="user", content=request),
    ]
